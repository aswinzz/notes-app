/*
 * NotesPage Messages
 *
 * This contains all the text for the NotesPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NotesPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NotesPage container!',
  },
});
